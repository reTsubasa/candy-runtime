# Explicit endpoint advertised to Candy Cloud for inbound SD-WAN connections.
CANDY_PUBLIC_ENDPOINT=203.0.113.10:18444
# Explicit local opt-in; omit for SD-WAN-only nodes.
# CANDY_PROXY_ARGS=--proxy-config /etc/candy/proxy-server.toml
